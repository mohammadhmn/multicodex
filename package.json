{
  "name": "multicodex-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "check": "bun run typecheck && bun run test && bun run build",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "release": "bun run --filter multicodex release",
    "release:cli": "bun run --filter multicodex release",
    "release:patch": "bun run --filter multicodex release --patch",
    "release:minor": "bun run --filter multicodex release --minor",
    "release:major": "bun run --filter multicodex release --major",
    "release:plan": "bun run --filter multicodex release --dry-run",
    "release:macos": "cd apps/macos && scripts/release.sh --bump patch",
    "macos:dev": "cd apps/macos && just dev",
    "macos:dmg": "cd apps/macos && just dmg",
    "macos:ci": "cd apps/macos && just ci"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "packageManager": "bun@1.3.9"
}
