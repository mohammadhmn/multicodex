# macOS menu bar app

The native Swift menu bar app now lives in a separate repository:

- `multicodex-mac`

This CLI repo is intentionally independent and does not include macOS app build or release automation.
